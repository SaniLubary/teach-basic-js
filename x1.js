// Corregir el codigo para que el resultado de este archivo JS al correrlo, sea 'true'
// Para poder correr el archivo, en una consola asegurate de estar parada en la carpeta de este archivo
// y posteriormente corre el comando 'node ./x1.js'

const name = 'steffita'
let result = false

if (name == 'steffita') {
    result = true
}

console.log(result)