/**
 * Crear un For loop que devuelva la suma
 * de todos los elementos del array y lo 
 * muestre en consola como un sólo número
 * 
 * Tip: Puedes crear nueva variable para 
 * almacenar el resultado
 */

const sumarme = [2,1,8,41,10]
let sum = 0;
for (let i = 0; i < sumarme.length; i++) {
   // hacer algo con el array
}
console.log ('El resultado es', sum);