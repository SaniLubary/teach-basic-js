/**
 * Crear un For loop que devuelva el valor más grande del array
 * 
 * Tip: Puedes crear nueva variable para 
 * almacenar el resultado
 */

const array = [3, 2, 6, 1, 4, 5];

for (let i = 0; i < array.length; i++) {
    // hacer algo
}