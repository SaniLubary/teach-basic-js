/**
 * Crear un for loop que ordene un array de menor a mayor y lo muestre en consola
 */

const array = [3, 2, 6, 1, 4, 5];

for (let i = 0; i < array.length; i++) {
    // hacer algo
}

console.log(array);